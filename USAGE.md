# 日常使用指南

安装、部署、首次配置请看 [`README.md`](README.md) 与 [`deploy/DEPLOY.md`](deploy/DEPLOY.md)。**本文只讲「已经跑起来了，平时怎么用」。**

---

## 0. 先确认你在哪一边

系统有桌面和服务器两种形态，数据是**各自独立**的 —— 服务器上的账号和本机的不是同一份，别搞混。

|              | 服务器（主力）                                                              | 桌面（Windows）                      |
| ------------ | --------------------------------------------------------------------------- | ------------------------------------ |
| 怎么启动     | 开机自动启动，不用管                                                        | 双击桌面「一键打卡」                 |
| 怎么开面板   | `ssh -L 5000:127.0.0.1:5000 user@server` 后打开 `http://127.0.0.1:5000` | 双击后自动开浏览器                   |
| 数据在哪     | `/opt/raricy/data/`                                                       | 项目目录下`runtime/`、`backend/` |
| 该看谁的日志 | 第 3 节的`docker logs`                                                    | 面板上的打卡记录表                   |

**两边的账号密码是分开维护的** —— 在服务器面板加了账号，本地不会有；反之亦然。

---

## 1. 每天其实什么都不用做

定时任务会在 **每天 00:00（北京时间）** 自动为所有**启用**的账号打卡并抽运势。正常情况下你不需要任何操作。

想确认它真的在跑，看第 3 节。

> 定时时间在「系统配置 → 定时设置」里改，保存后**立刻生效**，不需要重启容器。

---

## 2. 打开控制面板

### 服务器

```bash
# 在你自己的电脑上执行，保持这个窗口开着
ssh -L 5000:127.0.0.1:5000 user@server
```

然后在本地浏览器打开 `http://127.0.0.1:5000`。

隧道断了页面就打不开 —— 重新执行上面那条命令即可。**不要**把端口改成对外监听：面板没有登录验证，谁能打开它，谁就能读你的账号列表、改配置、删账号。

### 桌面

双击桌面「一键打卡」快捷方式。若服务已在运行，它只会打开浏览器，不会重复启动。

---

## 3. 查看今天打卡了没

### 面板

「控制中心」有今日状态总览；「自动打卡」面板有每个账号的详细状态和打卡记录表。

### 命令行（服务器上）

```bash
curl -s http://127.0.0.1:5000/api/status | python3 -c "
import sys, json
d = json.load(sys.stdin)
print('定时:', '已启用' if d['schedule_enabled'] else '已停用', d['schedule_times'], '下次:', d['next_scheduled'])
for user, rec in d['account_status'].items():
    if rec:
        print(f\"  [OK] {user}: {rec.get('message')}\")
    else:
        print(f\"  [--] {user}: 今日无记录\")
"
```

### 看日志

```bash
# 服务器：最近的打卡日志（JSON）
sudo docker exec raricy-checkin sh -c 'tail -c 2000 /app/runtime/logs/checkin_log.json'

# 服务器：容器自己的运行日志（排查启动/异常用这个）
sudo docker logs --tail 50 raricy-checkin
```

---

## 4. 手动补打卡

**什么时候需要**：某天服务没跑起来、或新加了一个账号想立刻打卡。

- 面板：「自动打卡」页，勾选账号 →「打卡所选」；或「批量全部」对所有启用账号打卡。
- 命令行：

```bash
# 单个或几个账号
curl -s -X POST http://127.0.0.1:5000/api/checkin \
  -H "Content-Type: application/json" \
  -d '{"accounts": ["user1", "user2"]}'

# 所有启用账号
curl -s -X POST http://127.0.0.1:5000/api/checkin \
  -H "Content-Type: application/json" \
  -d '{"accounts": ["all"]}'
```

接口立即返回一个 `task_id`，实际执行是异步的 —— 面板上会自动显示进度。

**重复打卡是安全的**：当天已打卡会返回「今日已打卡，无需重复操作」，不会重复计次。

---

## 5. 管理账号

在「系统配置 → 账号管理」：

| 想做的事     | 怎么做                                          |
| ------------ | ----------------------------------------------- |
| 加账号       | 底部输入用户名密码 →「添加账号」               |
| 暂停某个账号 | 点它右侧的「启用/禁用」徽章（禁用后不参与打卡） |
| 删除账号     | 点「移除」                                      |
| 改密码       | 把密码框改成新密码，保存                        |

**关于密码显示 `****`**：列表里密码永远是 `****`，这是脱敏显示。**保持 `****` 不动 = 保留原密码**；只有当你在框里输入了新内容，才会覆盖。

### 账号存哪、怎么备份

账号以**密文**存在 `backend/accounts.enc`，密钥是 `runtime/.accounts.key`（服务器上是 `/opt/raricy/key/accounts.key`）。

> **密钥丢了，账号就恢复不了，只能重新录入。** 请把密钥单独备份到别处 —— 别和 `data/` 放同一个备份包里（那样等于没加密）。

---

## 6. 改配置

「系统配置」页从上到下是账号、站点、选择器、定时、运势、API 路径。改完点页面底部的**「保存配置」**，写入磁盘并自动重启调度器，**立即生效**。

常用的两个：

- **定时时间**：勾选「启用定时打卡」，添加时间点（可多个）。默认 `00:00`。
- **运势卡片**：关掉「启用运势卡片选择」就不会在打卡后抽运势；「选择第几张」可设随机或固定。

> 「CSS 选择器」整块是旧版本遗留，引擎已不再读取，保持默认即可。

---

## 7. 博客工具

「博客工具」页三步：**扫描目录 → 抓取内容 → 批量点赞**。所有操作异步执行，页面显示实时进度。

需要注意两条站点限制：

- **点赞需要「核心用户（core）及以上」权限。** 普通用户账号点赞会返回 `403 需要核心用户权限`，记为该篇失败。
- **每日点赞上限 100 次/账号**（站点侧另有 100 次/小时、500 次/天的限制）。额度用完时面板会自动禁用点赞按钮。

---

## 8. 例行维护

### 备份（建议每周）

要备份**两个东西，分开存放**：

```bash
# 数据：配置 + 账号密文 + 打卡日志 + 博客库
sudo tar czf raricy-data-$(date +%F).tar.gz -C /opt/raricy/data .

# 密钥：单独存到别处
sudo tar czf raricy-key-$(date +%F).tar.gz -C /opt/raricy/key .
```

恢复见 `deploy/DEPLOY.md` 第 7 节。

### 看看服务还活着

```bash
sudo docker compose -f /opt/raricy/docker-compose.yml ps    # 应为 running / healthy
```

容器配了 `restart: unless-stopped`，崩溃或服务器重启后会自动拉起，通常不用管。

### 服务器重启后确认一下

```bash
# 重启后等一分钟，然后
curl -s http://127.0.0.1:5000/api/health
```

有响应就没问题；定时任务由容器内部调度，容器起来它就恢复了。

### 更新版本

见 `deploy/DEPLOY.md` 第 6 节。**要点是每次都要升版本号**，否则 `docker compose up -d` 可能不重建容器、继续跑旧的。

---

## 9. 出问题先看这里

| 现象                                                 | 先看什么                                                                                                                |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 面板打不开                                           | SSH 隧道断了吗？重新`ssh -L 5000:127.0.0.1:5000 user@server`                                                          |
| 面板能开但账号是空的 / 报`accounts_decrypt_failed` | 密钥与密文不配套。**先别在面板点保存**（数据不会被覆盖，但配置存不进去），检查 `/opt/raricy/key/accounts.key`   |
| 今天没自动打卡                                       | ① 容器在跑吗 ②「系统配置」里定时是启用状态吗 ③ 时区对不对：`sudo docker exec raricy-checkin date` 应是北京时间     |
| 某个账号打卡失败                                     | 面板记录里有原因。密码错误会明确写「用户名或密码错误」；提示「尝试过于频繁」是站点限频（15 分钟），**不是密码错** |
| 面板保存配置报错                                     | 服务器目录属主不对：`sudo chown -R 1000:1000 /opt/raricy/data`                                                        |
| 博客点赞全失败                                       | 该账号是不是 core 及以上权限？今日额度是不是用完了？                                                                    |

更多服务器侧的排错（SELinux、权限、更新）见 `deploy/DEPLOY.md` 第 8 节。

---

## 10. 三条安全底线

1. **面板没有鉴权。** 只通过 SSH 隧道访问，永远不要改成对外监听。
2. **密钥丢了账号就没了。** 单独备份 `accounts.key`。
3. **密钥和密文别放同一个备份里。** 加密防的正是「这一个文件单独泄露」，放一起就等于没加密。
