# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Runtime (logs / sqlite)
runtime/

# Legacy runtime dirs
backend/logs/

# Accounts (secrets, not committed)
backend/accounts.json
backend/accounts.enc

# Local tooling state & design docs
.superpowers/
.claude/
docs/
raricy/

# iOS generated (produced by xcodegen + xcodebuild, not committed)
ios/build/
ios/RaricyCheckin.xcodeproj/

# 部署用镜像 tar（docker save 的产物，约 54MB，`deploy/DEPLOY.md` 2.1 生成）
*.tar

scripts/
